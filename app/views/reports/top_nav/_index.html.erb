<div class="page-head print:hidden">
  <%= render partial: "reports/top_nav/period_indicator" %>
  <div class="flex lg:items-center gap-x-3">
    <% if @filter.is_custom_time_frame? %>
      <%= render partial: "reports/top_nav/custom_time_range_picker" %>
    <% else %>
      <%= render partial: "reports/top_nav/time_frame_selector" %>
    <% end %>
  </div>
</div>
