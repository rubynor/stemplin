<% if children.present? %>
  <% children.each do |group| %>
    <tr class="flex justify-between border-b border-slate-300 px-6 py-1.5 pl-<%= (level * 4) + 3 %> bg-slate-<%= level * 100 %>">
      <td>
        <span>
          <%= group[:title] %>
        </span>
      </td>
      <td>
        <span>
          <%= minutes_to_float group[:minutes] %>
        </span>
      </td>
    </tr>
    <% if group[:children].present? %>
      <%= render partial: "simple_report_section", locals: { children: group[:children], level: level + 1 } %>
    <% end %>
  <% end %>
<% end %>
