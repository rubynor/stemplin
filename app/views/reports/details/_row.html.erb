<% if children.present? %>
  <% children.each do |group| %>
    <tr class="flex justify-between border-b border-gray-100 py-4 pr-6 print:py-1 print:text-sm pl-[<%= (level * 10) + 25 %>px] bg-slate-<%= level * 100 %>">
      <td>
        <span>
          <%= group[:title] %>
        </span>
      </td>
      <td>
        <span>
          <%= minutes_to_float group[:minutes] %>
        </span>
      </td>
    </tr>
    <% if group[:children].present? %>
      <%= render partial: "reports/details/row", locals: { children: group[:children], level: level + 1 } %>
    <% end %>
  <% end %>
<% end %>
