<div class="flex flex-col gap-y-1">
  <%= image_tag "tada.png", class: "w-12 h-12" %>
  <span class="font-semibold text-xl text-gray-700">Welcome! Change password?</span>
  <span class="font-regular text-base text-gray-500">This is an optional change. If you'd like to create your own password instead of using the generated one, feel free to proceed. Otherwise, you can skip this step.</span>
  <div id="onboarding_edit_password" class="mt-6">
    <%= render partial: "onboarding/edit_password_form", locals: { user: @user } %>
  </div>
</div>
