<!DOCTYPE html>
<html>
  <head>
    <title>Reap</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= hotwire_livereload_tags if Rails.env.development? %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
  </head>
  <body class="font-montserrat">
  <!-- Navbar -->
    <%= render 'layouts/navbar'%>
    <%= render partial: "shared/flash_messages" %>

    <div class="flex justify-evenly pt-4 print:hidden">
      <% if notice %>
        <p class="notice bg-green-300 text-red-900 px-3 py-3 w-fit rounded-full"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert bg-red-300 text-red-900 px-3 py-3 w-fit rounded-full"><%= alert %></p>
      <% end %>
    </div>

    <main>
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="print:hidden bottom-0 h-16 items-center flex space-x-2 sm:justify-center fixed w-full">
      <a href="" class="font-bold">Reap</a>
    </footer>
  </body>
</html>
