<% content_for :base_layout do %>
  <main>
    <div class="grid grid-cols-1 lg:grid-cols-2 lg:h-screen">
      <div class="bg-gray-50 h-full relative">
        <div class="lg:absolute lg:top-0 lg:left-0 w-full">
          <div class="flex items-center w-full justify-between max-w-xl mx-auto px-4 py-6 lg:py-10">
            <%= link_to root_url, target: "_top", class: "text-2xl flex items-center gap-x-2" do %>
              <%= image_tag "stemplin-logo-dark.svg" %>
            <% end %>
          </div>
        </div>
        <div class="max-w-xl mx-auto h-full flex items-center p-4">
          <%= yield %>
        </div>
      </div>
      <div class="hidden lg:block shadow-xl auth-illustration-wrapper relative">
        <div class="max-w-xl mx-auto p-4 flex flex-col justify-center items-center h-full">
          <%= image_tag "feature.svg" %>
        </div>
      </div>
    </div>
  </main>
<% end %>

<%= render template: "layouts/base" %>
